[project]
name = "smc-trading-engine"
version = "0.1.0"
description = "Deterministic SMC decision/orchestration engine"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
