services:
  - type: worker
    name: cryptomentor-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 main.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
