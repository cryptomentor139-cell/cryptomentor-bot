🚨 My CryptoMentor AI project is having the following critical issues that need your smart debugging and code injection:

---

## 🔧 ISSUE 1: Coinglass V4 Data Not Showing

In all `/analyze`, `/futures`, and `/market` commands, this error appears:
> 📊 5. Futures Insight (CoinGlass)
> • ⚠️ Data CoinGlass tidak tersedia sementara
> • Menggunakan estimasi berdasarkan trend pasar

This means the data is not fetched properly from Coinglass V4 API. In my secrets, the key is stored as: `COINGLASS_API_KEY`.

✅ Ensure the following:
- The base URL being used is: `https://open-api-v4.coinglass.com/public/v4/futures`
- The `X-API-KEY` header is correctly passed in all requests.
- The following endpoints must be handled and parsed correctly:
  - `/longShortRate`
  - `/openInterest`
  - `/fundingRate`
  - `/liquidationMap`
- The symbol mapping must use `BTC`, `ETH`, etc. directly — **not** "binance_btcusdt" or anything exchange-prefixed.

---

## 🔧 ISSUE 2: Missing or Undefined Methods in `CryptoAPI`

I'm getting this log constantly:
> 'CryptoAPI' object has no attribute 'get_binance_long_short_ratio'

✅ Please:
- Inject or define the following methods inside the `CryptoAPI` class:
  - `get_binance_long_short_ratio(symbol)`
  - `get_binance_open_interest(symbol)`
  - `get_binance_funding_rate(symbol)`
  - `get_liquidation_zones(symbol)`
  - `analyze_supply_demand(symbol)` *(for SMC/Smart SnD)*

These should pull data from CoinGlass V4 and return usable data structures for signals.

---

## 🔧 ISSUE 3: Telegram Markdown Error in `/market`

Here’s the full error:
> Error: Can't parse entities: can't find end of the entity starting at byte offset 116...

✅ Fix:
- This is a MarkdownV2 escaping issue in Telegram.
- Scan all `bot.send_message()` or `bot.edit_message_text()` lines and ensure:
  - Reserved MarkdownV2 characters like `_`, `*`, `[`, `]`, `(`, `)`, `~`, `>`, `#`, `+`, `-`, `=`, `|`, `{`, `}`, `.`, `!` are properly escaped.
  - Or as fallback: send as `parse_mode='HTML'` or plain text when Markdown fails.

---

## ✅ What I Expect From You

I want you to:
1. Analyze the existing code of `CryptoAPI` and telegram handlers.
2. Patch any missing methods dynamically (or redefine the class).
3. Validate that all CoinGlass V4 endpoints return valid data and display correctly.
4. Fix all markdown-related Telegram errors gracefully.
5. Return a short summary: ✅ What was fixed and how many methods were patched.

💡 Please reason like an engineer and solve the root cause, not just symptomatically.

I'm trusting your logic. You may edit multiple files/classes as needed.
