ROLE
Kamu adalah AI Agent yang memperbaiki error:
SyntaxError: invalid character '•' (U+2022)
pada file Python. Fokus hanya pada perbaikan karakter non-ASCII di source.

INPUT KONTEKS MINIMAL
- File target utama: /home/runner/workspace/Bismillah/bot.py
- Error lokasi: line 1929
- Contoh baris bermasalah:
  "• SUPABASE_URL: {'Valid' if os.getenv('SUPABASE_URL') else 'Missing'}"

TUJUAN
1) Hilangkan semua karakter non-ASCII yang bisa memicu SyntaxError (bullet •, emoji, dsb).
2) Ganti semua bullet/simbol non-ASCII dengan ASCII aman (mis. '-' atau '*').
3) Pastikan file Python valid di-parse dan dapat di-import tanpa SyntaxError.

ATURAN PERBAIKAN (MANDATORY)
- ASCII-ONLY: Semua string literal dalam kode harus hanya berisi ASCII.
- Ganti '•' (U+2022) menjadi '-'.
- Ganti simbol non-ASCII lain (mis. '–', '—', '✓', '✅') menjadi padanan ASCII ('-', '[OK]') atau hapus.
- Jangan ubah logika Python (hanya konten string/komentar).
- Jangan tambahkan emoji baru.
- Tambahkan deklarasi encoding UTF-8 di baris pertama file jika belum ada:
  # -*- coding: utf-8 -*-

OUTPUT YANG DIHARAPKAN
- Kembalikan patch dalam bentuk unified diff (ASCII-only), atau
- JSON berisi daftar penggantian:
  {
    "file": "Bismillah/bot.py",
    "replacements": [
      { "line": 1929, "from": "• SUPABASE_URL: {...}", "to": "- SUPABASE_URL: {...}" },
      ...
    ]
  }

CHECKLIST PASCA-PERBAIKAN
- Lakukan parse test (mental check): tidak ada karakter non-ASCII tersisa di literal terkait.
- Pastikan import "from bot import TelegramBot" tidak memicu SyntaxError.
- Pertahankan isi f-string/formatting Python, ganti hanya karakter display.

CONTOH PERBAIKAN LANGSUNG (TEMPLATE)
Ganti blok teks status yang sebelumnya memakai bullet non-ASCII menjadi ASCII-only, misalnya:
Sebelum:
  """Config Check
  • SUPABASE_URL: {url_status}
  • SUPABASE_SERVICE_ROLE_KEY: {key_status}
  """
Sesudah:
  """Config Check
  - SUPABASE_URL: {url_status}
  - SUPABASE_SERVICE_ROLE_KEY: {key_status}
  """

KESALAHAN UMUM YANG HARUS DICEGAH
- Menyisakan satu karakter non-ASCII pun di string Python.
- Mengubah struktur kode atau ekspresi Python di dalam f-string.
- Menambahkan emoji/simbol baru.

JIKA PERBAIKAN BERHASIL
- Laporkan ringkas (ASCII-only) dengan salah satu format output di atas.
